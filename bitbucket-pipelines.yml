# Testing ResFinder

image: python:3.9

pipelines:
  custom:
    build-and-test:
      - step:
          name: Test
          caches:
            - pip
          script:
            - pip install --upgrade pipenv wheel
            - pipenv install --dev
            - pipenv run build
            - pipenv run test
